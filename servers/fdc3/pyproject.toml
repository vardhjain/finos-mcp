[project]
name = "finos-mcp-fdc3"
version = "0.1.0"
description = "Read-only MCP server exposing the FINOS fdc3 standard as typed tools"
readme = "README.md"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = ["finos-mcp-core"]

[project.scripts]
finos-mcp-fdc3 = "finos_mcp.fdc3.__main__:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/finos_mcp"]
